# Tiles = [] # 전체 바닥 타일들의 ( 중앙x, 중앙y, 벽이 있나 없나, 아이템이 있나없나)값을 저장한 리스트
# insert_tile = []
#
# for column in range(1, 14):
#     for row in range(1, 16):
#         insert_tile = [55+60*(row-1), 810-60*(column-1), False, False]
#         Tiles.append(list(insert_tile))
